using System.Collections;
using System.Collections.Generic;
using System.Linq;
using Emir;
using Unity.VisualScripting;
using UnityEngine;

public class PickaxesManager : MonoBehaviour
{
  //  private GameSettings gameSettings => GameManager.Instance.GetGameSettings();


    // private void Initialize()
    // {
    //     foreach (MetalData Data in gameSettings.PickaxesData)
    //     {
    //         for (int i = 0; i < Data.PoolCount; i++)
    //         {
    //            PickaxeComponent Pickaxes= gameSettings.PickaxesPrefabs.SingleOrDefault(x => x.GetEGemType() == Data.Type);
    //            
    //         }
    //     }
    // }
}